{
  "name": "game-catalog-validator",
  "version": "0.1.0",
  "private": true,
  "description": "TECH-3635 PoC — validates that games exposed by Casino V2 for a brand+region can actually be launched",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "validate": "node src/run.js",
    "validate:browser": "node src/validate-browser.js"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
